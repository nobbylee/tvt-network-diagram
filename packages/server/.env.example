# 本工具自己的会话 JWT 密钥（跟主平台无关，随便改成一个随机字符串即可）
JWT_SECRET=tvt-dev-secret-change-me

# 免密登录（SSO handoff）用：必须和「TVT技术服务部平台」server/.env 里的 JWT_SECRET 配成完全一样的值，
# 否则从主平台跳转过来时 POST /api/auth/handoff 会全部校验失败（401）。
EXAM_SSO_SECRET=

# 可选，默认见 src/index.ts 里的 fallback
# PORT=3001
# HOST=0.0.0.0
# DATA_DIR=./data
